<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>PokeInfo</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
  <nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container">
      <a class="navbar-brand fs-3 fw-bold text-primary" href="/">PokeInfo</a>
      <div class="d-flex ms-auto">
        <form action="/search" method="GET" class="d-flex">
          <input type="text" name="q" class="form-control me-2" placeholder="Search Pokemon..." required>
          <button type="submit" class="btn btn-primary">Search</button>
        </form>
        <a href="/favorites" class="btn btn-secondary ms-2">Favorites</a>
      </div>
    </div>
  </nav>

  <div class="container mt-4">
    <% if (locals.error) { %>
      <div class="alert alert-danger">
        <%= error %>
      </div>
    <% } %>

    <div class="welcome-card text-center py-5">
      <h1 class="display-3 fw-bold text-primary mb-4">Welcome to PokeInfo</h1>
      <p class="fs-5 text-secondary mb-5">
        Search for any Pokemon by name or ID number to view detailed information.<br>
        Save your favorite Pokemon for quick access later.
      </p>

      <div class="suggestions mt-5">
        <h5 class="text-secondary mb-3">Try searching for:</h5>
        <div class="d-flex justify-content-center gap-2">
          <% suggestions.forEach(suggestion => { %>
            <a href="/search?q=<%= suggestion %>" class="btn btn-outline-info"><%= suggestion %></a>
          <% }); %>
        </div>
      </div>
    </div>
  </div>

  <footer class="fixed-bottom py-2 bg-secondary text-white">
    <div class="container">
      <p class="m-0" id="status-text">Enter a Pokemon name or ID to search</p>
    </div>
  </footer>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
  <script src="/js/main.js"></script>
</body>
</html>